{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true
  },
  "include": ["packages", "themes", "apps", "jest.setup.ts", "vite.config.ts"],
  "exclude": ["node_modules", "**/dist", "**/build"]
}
